⚫●◯○◦◉◍◑◙◎◌
⚫数组理论基础

●数组在内存的空间地址是连续的
那么二维数组在内存的空间地址是连续的么？
不同编程语言的内存管理是不一样的，以C++为例，在C++中二维数组是连续分布的。

⚫704. 二分查找
循环不变量：同逻辑操作中，某些本质一样的参数须保持一致
如区间开闭和判定范围须保持一致

⚫35.搜索插入位置
只要看到数组是有序数组，都可以想二分法
数组中无重复元素，因为一旦有重复元素，使用二分查找法返回的元素下标可能不是唯一的。

⚫34.在排序数组中查找元素的第一个和最后一个位置 (opens new window)
扎扎实实的写两个二分分别找左边界和右边界
最后判断特殊情况返回无解输出
【？】rightBorder = left;

⚫27. 移除元素
通过一个快指针和慢指针在一个for循环下完成两个for循环的工作

⚫977.有序数组的平方
nums_new = [float('-inf')] * len(nums)  # 需要提前定义列表，存放结果，float('inf')指无穷大；float('-inf')指无穷小

⚫209.长度最小的子数组
所以 只用一个for循环，那么这个循环的索引，一定是表示 滑动窗口的终止位置
不要以为for里放一个while就以为是O(n^2)啊， 主要是看每一个元素被操作的次数，每个元素在滑动窗后进来操作一次出去操作一次，每个元素都是被操作两次，所以时间复杂度是 2 × n

⚫59.螺旋矩阵II
本题依然是要坚持循环不变量原则
第一步，找循环体和循环次数，先看n为4，5，6的情况找出共性（n较少时不宜总结共性）。共性：每次循环一圈，要循环n/2圈，如果n为奇数，最后还需要一个中间点
第二步，边写循环边找变量的通式，如起止点位置和循环次数有关，用例子验证
第三步，增加特殊情况，结尾判定奇偶性填充中心点

